name: release-please

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: reggi/release-please-action@1ab4d7f23135249e2efd8b12b19e8e3ae85ad47d
        with:
          token: ${{ secrets.MY_RELEASE_PLEASE_TOKEN }}
          config-file: 'release-please-config.json'
          manifest-file: '.release-please-manifest.json'
